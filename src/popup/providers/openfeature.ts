import type { ProviderMeta } from '../../types.js'

export const meta: ProviderMeta = {
  id: 'openfeature',
  name: 'OpenFeature',
  imageSrc: 'assets/openfeature.png',
  lightBadge: true,
  logoOnly: true,
}
